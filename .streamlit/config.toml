[server]
# Disable the file watcher entirely. Streamlit's watcher inspects all imported
# module paths at startup, which triggers lazy imports in `transformers` that
# require `torchvision` (not installed). Setting "none" eliminates the noise.
# Restart the app manually after code changes.
fileWatcherType = "none"
